    <div class="container">
      <div class="post-list">
        <div class="post">
          <div class="user-name">
            <%= @targated_post.author.name %>
            <span class="user-post-count">
              <%= @targated_post.comments_counter %> Comments <%= @targated_post.likes_counter %> Likes
            </span>
          </div>
          <p class="post-text"><%= @targated_post.text %></p>
          <div class="like-button">
            <% if current_user %>
              <% if current_user.likes.exists?(post: @targated_post) %>
                <%= link_to "Unlike", post_like_path(@targated_post, current_user.likes.find_by(post: @targated_post)), method: :delete, class: 'btn btn-danger' %>
              <% else %>
                <%= link_to "Like", post_likes_path(@targated_post), method: :post, class: 'btn btn-primary' %>
              <% end %>
            <% end %>
          </div>
        </div>
        <div class="comment-container">
          <% @comments.each do |comment| %>
            <div class="comment">
              <span class="comment-username"><%= comment.author.name %>:</span>
              <span class="comment-text"><%= comment.text %></span>
            </div>
          <% end %>
        </div>
      </div>
    </div>
